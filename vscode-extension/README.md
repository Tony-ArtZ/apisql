# ApiSQL VS Code Extension

Language support for ApiSQL.

## Features

- Syntax highlighting
- Language Server Protocol (LSP) support
- Code completion
- Diagnostics

## Requirements

This extension requires the `lsp-backend` binary. It is included in the extension for supported platforms.
